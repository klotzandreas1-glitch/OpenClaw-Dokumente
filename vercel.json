{
  "buildCommand": "cd steuer-assistent && npm install && npm run build",
  "outputDirectory": "steuer-assistent/dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
