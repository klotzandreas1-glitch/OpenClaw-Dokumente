{
  "buildCommand": "cd steuer-assistent && npm run build",
  "outputDirectory": "steuer-assistent/dist",
  "installCommand": "npm install && cd steuer-assistent && npm install",
  "functions": {
    "api/submit.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
